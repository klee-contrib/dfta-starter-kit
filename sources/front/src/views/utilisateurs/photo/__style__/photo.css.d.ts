import {CSSElement} from "@focus4/styling";

interface Display { _2b435: void }
interface Picker { _846ea: void }

export interface PhotoCss {
    display: CSSElement<Display>;
    picker: CSSElement<Picker>;
}

declare const photoCss: PhotoCss;
export default photoCss;
