export const router = {
    root: "Home",
    profils: {
        root: "Profiles",
        proId: {
            root: "{{param}}"
        }
    },
    utilisateurs: {
        root: "Users",
        utiId: {
            root: "{{param}}"
        }
    }
};
