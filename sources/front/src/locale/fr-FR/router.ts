export const router = {
    root: "Accueil",
    profils: {
        root: "Profils",
        proId: {
            root: "{{param}}"
        }
    },
    utilisateurs: {
        root: "Utilisateurs",
        utiId: {
            root: "{{param}}"
        }
    }
};
